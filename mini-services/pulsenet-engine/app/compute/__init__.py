"""Pure deterministic compute (no I/O, no LLM) — trivially unit-testable.

LLMs parse chaos; THIS package does the math. Every function here is a pure
function so a failing test points at exactly one formula.
"""
